# 统一换行符：仓库里一律存 LF，各平台检出时由 git 处理。
# 不加这个文件的话，换一台 core.autocrlf 设置不同的机器，
# 随手一改就会产生「整个文件都变了」的巨型 diff。
* text=auto eol=lf

# 这些不能被当成文本处理，否则会被改坏。
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.avif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary

# lockfile 冲突交给 npm 解，不要手动合并。
package-lock.json -diff
